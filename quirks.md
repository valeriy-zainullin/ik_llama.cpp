# Quirks: AMX-INT8 в ik_llama.cpp (ветка amx-gemm)

Особенности платформы (Sapphire Rapids, GCC 14.2.0, `-march=native`, Linux),
компиляторов (GCC 12–16) и нашего кода, обнаруженные при отладке
`ggml/src/ggml-amx.cpp`. Всё ниже подтверждено экспериментально через
`./build-amx/bin/test-amx-gemm diag` на AMX-сервере.

## 1. Интринсики AMX в GCC — «голый» asm без семантики

Через все известные версии GCC (12 … master, в отличие от clang) `_tile_loadd`,
`_tile_dpbssd`, `_tile_zero`, `_tile_stored` — это голые `asm volatile` без
смоделированных эффектов:

- `tileloadd` не объявляет ни memory-операнда, ни clobber → компилятор может
  поднять его **выше** записи данных в буфер (получим старые/нулевые данные);
- `tdpbssd`/`tilezero` не имеют операндов вообще, tmm-регистры для
  регистрового аллокатора не существуют → последовательность `tilezero` →
  `tdpbssd` может быть переставлена (нулевые результаты).

**Решение:** собственные обёртки `AMX_TILE_*` с фиктивной read-write
зависимостью `"+r"(tok)`, продетой через **каждую** тайловую операцию
(реальная data-dependency не ломается планировщиком и LICM), плюс
`"memory"` clobber на loadd/stored. См. комментарий в `ggml-amx.cpp`.

## 2. Номера тайлов — только литералы

У GCC интринсики — макросы со стрингификацией (`"%%tmm"#dst`), поэтому номер
тайла обязан быть целочисленным литералом **после препроцессора**: enum/
constexpr даёт `%tmmTMM0` и ошибку ассемблера. Нужны `#define TMM0..7` и
двухуровневые макросы (оператор `#` подавляет раскрытие аргумента, поэтому
внешний макрос обязан развернуть его первым — тот же трюк, что у самого GCC).
llama.cpp делает так же (`#define TMM0..7` в `ggml-cpu/amx/common.h`).

## 3. Нумерация операндов в своих asm-обёртках

`tok` — это `"+r"` выход (`%0`), поэтому base/stride — `%1`/`%2`. Страйд
обязательно кастовать в `(size_t)(stride)`: без него на части путей GCC 12
пытался загрузить 64-битный регистр из 32-битного значения. Токен — один
`unsigned` на всю функцию, сквозной через все операции.

## 4. TILECFG

Layout: `palette` u8 @0, `start_row` u8 @1, `colsb[8]` u16 @16, `rows[8]` u8
@48, `alignas(64)`; palette=1, start_row=0. Наша конфигурация: tmm0/tmm1 —
8×64 (B, VNNI), tmm2/tmm3 — 16×32 (A), tmm4..7 — 16×64 (C, int32).
`ldtilecfg` тоже emitted своим asm с `"memory"`, иначе конфиг-сторы могут
«уехать» ниже. Проверяется roundtrip'ом через `sttilecfg` (diag, бит 1).

## 5. tileloadd с некратным 32 страйдом: аномалия readback (контент при этом верен)

Эксперименты (diag шаги 7/8, сервер):

- `loadd` со страйдом 34/68 → `stored` → `memcmp`: **FAIL** (строка 0 тайла
  совпадает, строки 1+ — нет). Страйды 32/64 — OK. Не зависит от выравнивания
  базы (+2/+32), от solo/pair/interleaved последовательности.
- НО: тот же `loadd` (страйд 34) → `dpbssd` → результат **полностью верен**
  (старый шаг 7, блок t=0 прошёл целиком). И 7b-снапшоты: tmm1 не «теряется»
  ни на одной стадии последовательности.

Вывод: порча **контента** страйдом не подтверждена; подтверждена аномалия
пары `stored`→`memcmp` при некратном страйде загрузки. Природа не выяснена.
Полагаться на readback-проверки с некратными страйдами нельзя; для
перекрёстной проверки контента использовать `dpbssd` с identity-A
(7a-подход), а не `stored`.

**Решение в кернеле:** A всегда стагерится через выровненный буфер
(`unpack_a_tail`, memcpy) и грузится со страйдом 32. Стагеринг в любом
случае нужен для хвоста M и стоит 2×512 Б memcpy на окно.

## 6. «Потерянный тайл» — ЛОЖНЫЙ след; настоящий баг был в раскладке индексов

Долгое время считали, что тайл «забывается», если между `loadd` и
использованием есть другие тайловые операции (симптом: ровные нули в
колонках ≥16 при пустой второй A-половине, M=8/9; в строках ≥16 при
nv1=0, M=33). Снапшоты 7b опровергли это: tmm1 цел на **каждой** стадии.

Настоящая причина: **несовпадение раскладки C-тайлов между блоком
`tilestored` и `acc_4tiles`**:

- stored пишет `[cur0..3] = [A0B0, A1B0, A0B1, A1B1]` (бит0 t = половина строк),
- acc читал `mb=16*(t>>1), nb=16*(t&1)` (бит0 t = половина колонок).

В результате `acc` умножал содержимое «чужой» ячейки на шкалу другой
половины. Пустая половина давала **точный 0.0** — неотличимо от «потерянного
тайла». Численная сверка (7b): `got=-1104` = Σa[16]·w[0] (содержимое верно
для своей раскладки), `exp=-5456` = Σa[0]·w[16] (ожидание acc).

**Урок:** раскладка индексов C-тайлов должна быть единой; при отладке AMX
сначала сверять сырые int-произведения с ЯВНОЙ раскладкой (7b), и только
потом обвинять железо. Порядок load→use в кернеле сохранён как бесплатная
предосторожность (совпадает с апстримом), но не является исправлением чего-либо.

## 7. VNNI-упаковка B и порядок операндов tdpbssd

- B-тайл: строка `kg` (0..7), байты `[n*4+b]` = `qs(w_n, kg*4+b)`.
  Получается трюком «8×8 транспонирование int32»: `v[j]` — 32 байта строки
  w (q8_0) либо 16 nibbles q4_0; после `transpose_8x8_32bit` — `t[j]` в
  `tile + j*64 + h*32` (h — половина колонок). Шкалы — 16 fp16 в байтах
  512..543 того же буфера.
- A-тайл: 16×32B row-major, активации q8_0, dpbssd суммирует по k=0..31.
- ATT-порядок операндов у GCC: `tdpbssd %tmm_src2, %tmm_src1, %tmm_dst`
  (см. макрос `AMX_TILE_DPBSSD_I`), Intel: `tdpbssd dst, src1, src2`.
- Конвенция q4_0 — **split** (в ik_llama.cpp И в апстриме llama.cpp):
  элементы 0..15 → младшие nibbles байтов 0..15, элементы 16..31 → старшие
  (см. `quantize_row_q4_0_ref`, `ggml-quants.c`; у апстрима то же самое —
  `bytes_from_nibbles_32` в amx/mmq.cpp). Не перепутать с «interleaved
  парами» (x[2j], x[2j+1] в одном байте) — это другая упаковка, здесь её нет.

## 8. Мелочи рантайма и сборки

- Доступ к AMX: CPUID 7.0 EDX bits 22 (AMX-TILE) / 25 (AMX-INT8) +
  XGETBV XCR0 bits 17/18 + `arch_prctl(ARCH_REQ_XCOMP_PERM, 18)`; кэш в
  `std::atomic`. Отключение: `GGML_AMX_DISABLE=1` или `GGML_AMX=0`.
- `thread_local` POD-структуры — в `.tbss`, т.е. **нуль-инициализированы**
  (поле `flip` = 0, мусора нет).
- `GGML_FP16_TO_FP32` в ik_llama.cpp — табличный (`ggml_table_f32_f16`):
  любой тест/инструмент обязан вызвать `ggml_init()` до использования,
  иначе все конверсии дают мусор.
- `ggml-amx.cpp` должен всегда попадать в сборку (не только при AMX-флагах),
  иначе link error на не-x86: экспорты `ggml_amx_*` с фоллбэками.
  Per-file флаги через `check_cxx_source_compiles` + `set_source_files_properties`.
- Диагностические синтетические данные должны заполняться с тем же страйдом
  строк, с каким их читает кернел (`34*KB`, а не `34`) — иначе «баги» в
  самом diag (было в шаге 6b).
- Если memcmp тайла падает, но hex-дамп первой строки совпадает — порча в
  поздних строках; печатать надо всю строку 1, а не только строку 0.

## 9. Окружение

- Сервер: GCC 14.2.0, ccache, build dir `build-amx`, скрипт
  `scripts/benchmark-amx.sh` (build + correctness + bench).
- Локально: GCC 16.2.1 / clang 22, AVX2 есть, **AVX512/AMX нет** → локально
  только компиляция и дизассемблер; тест завершается "AMX is not available".
  Локальная сборка: `/tmp/opencode/build-amx`.
- **Проверять код возврата сборки** (`echo "exit=$?"`): grep по "error"
  не ловит русские "Ошибка" — дважды пропускали ошибки сборки.
- Локальный кодоген GCC 16 был корректен, серверный GCC 14 — нет: локальный
  objdump не гарантирует поведение сервера; при необходимости просить
  `objdump -d build-amx/bin/test-amx-gemm` на сервере.

## 10. Диагностика: карта битов возврата `test-amx-gemm diag`

| бит  | тест                                                        |
|------|-------------------------------------------------------------|
| 1    | TILECFG roundtrip (sttilecfg)                               |
| 2,4,8,128 | roundtrip tmm0/tmm2/tmm4 и tmm1/tmm3/tmm5/tmm6/tmm7    |
| 16   | tdpbssd для всех 4 комбинаций A×B (tmm2/tmm3 × tmm0/tmm1)   |
| 32   | pack_b_tile в thread_local буфер + умножение (bufmul)       |
| 64   | кернел 16×16 (одна пара тайлов) на синтетике                |
| 256  | кернел на формах из correctness (8×32×64 … 31×47×32), дамп строки |
| 512  | back-to-back loadd tmm0..3 + readback (реплика внутренностей кернела) |
| 1024 | блок 4×(zero+dpbssd+stored) на упакованных данных           |
| 2048 | матрица solo/pair/interleaved/quad (страйды 32/34/68, базы +2/+32) |

Уроки: изолированные по-инструкционные тесты проходили при битом кернеле;
решающую информацию дали (а) дословная реплика последовательности кернела
и (б) факторная матрица «страйд × база × длина серии».

## 11. Виртуализация: CPUID врёт про AMX-BF16

- Сервер amx-zainullin — KVM-VM, Xeon Platinum 8581C (Granite Rapids,
  family 6 model 207). `lscpu` показывает флаги `amx_bf16 amx_tile
  amx_int8`, НО инструкция `tdpbf16ps` даёт #UD (SIGILL, rc=132).
- Проверено изолированным пробником (config: tmm0/tmm1 16x32, tmm2 16x64,
  palette 1 — формы валидны и совпадают с SDM-примерами; tilezero/
  tileloadd из того же набора работают): #UD именно на tdpbf16ps.
- Вывод: флаг CPUID скопирован с хоста, но исполнение через KVM не
  проведено (старый KVM/QEMU или фильтрация фич). **Флаги CPUID под
  виртуализацией — не доказательство; пробовать исполнением** (sigsetjmp
  + SIGILL-хендлер), в коде это `amx_bf16_executes()`.
- Следствие: bf16-кернел на этой VM недоступен в принципе; INT8-вердикт
  из amx-analysis.md для неё окончателен. На bare metal Granite Rapids
  tdpbf16ps исполняется — bf16-ветка оставлена для железа без гипервизора.

## Текущее состояние

- correctness: ВСЕ тесты зелёные (32/32, Q4_0+Q8_0, nth=1/4, tails) на
  фикс-раскладке. End-to-end: генерация IDENTICAL, PPL 20.2264 ± 0.16711
  в обоих режимах (TinyLlama Q4_0, wiki.test).
- Производительность (TinyLlama 1B, 2 потока): pp512 ≈ 104 t/s в ОБОИХ
  режимах, кернел ≈ 120-143 GOPS (2 потока) ≈ 48 MAC/такт/ядро — уровень
  IQK, ускорителя AMX занят ~12% времени. Причины найдены сравнением с
  апстримом (amx/mmq.cpp): (а) у нас acc_4tiles стоял ПОСЛЕ всех
  stored (сериализация: AMX простаивал во время AVX512-скалирования и
  наоборот), (б) A стагерился через буфер (гигабайт memset+copy на GEMM).
- Внесено: расписание апстрима — acc_one_tile (выделен из acc_4tiles)
  вызывается после КАЖДОГО stored, прячется в тень следующего умножения;
  полные A-тайлы грузятся ПРЯМО из активаций (row step 34*KB; вычисление
  верно — доказано нашими diag-пробами, врёт только memcmp-readback,
  раздел 5), стагеринг остался только для M-хвостов (mr<16).
  d116fdc2: correctness 32/32 зелёная.
- РЕЗУЛЬТАТ (TinyLlama 1B, 2 потока, d116fdc2): кернел Q4_0 120 -> 145,
  Q8_0 143 -> 188 GOPS (+21..31%). End-to-end pp512: Q4_0 — паритет
  (103.36 vs 103.71); Q8_0 — ПЕРВЫЙ ВЫИГРЫШ: 106.84 ± 0.27 (AMX) vs
  102.93 ± 6.37 (IQK), ~+3.8% и стабильнее. tg128 не меняется (gate M>=8).
  Почему q4 в паритете, а q8 выигрывает: наш кернел ~72 GOPS/поток (q4)
  против ~114 GOPS/поток у IQK — упаковка нибблов съедает разницу; у q8
  кернел 188 GOPS, упаковка проще — уже хватает на +3.8%.
- Потолок текущей INT8-схемы: на каждый k-блок (K=32) — 4 dpbssd
  против ~200 uop упаковки B + пересчёта C (fp16-шкалы каждые 32 k).
  Это общий потолок схемы и у апстрима. Следующие рычаги:
  1) преупаковка B с кэшем (у апстрима; снимает транспонирование из
     каждого вызова, Q4_0 подтянуть к уровню Q8_0 ~190 GOPS) —
     недостаточно для победы в pp512;
  2) BF16-путь (dpbf16ps, fp32-накопление по всему K без пересчёта) —
     режим, где AMX реально насыщается; потребует новый кернел и
     перепроверку PPL (веса округляются до bf16);
  3) или зафиксировать результат: INT8-путь корректен, competitive
     с IQK в сыром виде, в pp512 — паритет; AMX включается по env.
- BF16-ветка (внесена, но на сервере НЕисполнима — раздел 11): кернел
  `amx_gemm_bf16_impl` — tdpbf16ps, шкалы вшиваются при упаковке
  (pack_b_tile_bf16, 16x16 bf16 row-major БЕЗ VNNI, k-чанк = 16),
  накопление fp32 живёт в C-тайлах весь K; A конвертируется q8_0 -> bf16
  один раз в thread_local буфер (convert_a_bf16); полные тайлы пишутся
  tilestored ПРЯМО в dst (перекрытие не нужно - каждый элемент
  вычисляется один раз), хвосты через masked add. Конфиг тайлов стал
  двувариантным (int8: 8x64 B / bf16: 16x32 B; переключение конфига
  отслеживается на поток). Выбор кернела: по умолчанию bf16, но только
  если `amx_bf16_executes()` (исполнительная проба, раздел 11) — иначе
  автоматический откат на int8; GGML_AMX_INT8=1 / ggml_amx_set_int8(1)
  форсируют старый путь. Тесты пропускают bf16-секцию, если инструкция
  не исполняется (ggml_amx_has_bf16). Ожидание для bare metal: 200+
  GOPS; проверить PPL (веса округлены до bf16). Итоги в amx-analysis.md.
- Ветка `amx-gemm`, один amend-коммит, пуш `--force-with-lease`.
