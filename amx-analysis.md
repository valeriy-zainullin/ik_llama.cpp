# AMX в ik_llama.cpp: итоги эксперимента на Q4_0/Q8_0 (INT8-путь)

Дата: сентябрь 2026. Ветка `amx-gemm`. Смежные документы: `quirks.md`
(технические особенности железа), комментарии в `ggml/src/ggml-amx.cpp`.

## Что было построено

AMX-кернелы (Intel Advanced Matrix Extensions, тайловый матричный
умножитель) для GEMM `dst = A × Wᵀ` с весами Q4_0/Q8_0 и float-активациями:

- INT8-путь (`tdpbssd`): активации квантизуются в q8_0, веса пакуются в
  VNNI-порядок, произведение накапливается в int32-тайлах, после каждого
  блока K=32 пересчитывается в float с fp16-шкалами.
- BF16-путь (`tdpbf16ps`, по итогам признан основным): веса и активации
  один раз деквантуются в bf16 (шкалы вшиваются), накопление fp32 идёт по
  всему K внутри тайла — без пересчётов.
- Диспетчер в `ggml.c`, переключение через `GGML_AMX_DISABLE=1`,
  выбор кернела через `GGML_AMX_INT8=1`. Полная correctness-обвязка.

Корректность: 32/32 тестов (обе ветки, оба типа, хвосты, nth=1/4),
end-to-end perplexity совпадает с IQK-путём до 5-го знака (INT8).

## Замеры (TinyLlama-1.1B, сервер amx-zainullin, 2 потока)

Сырой кернел (M=512, N=4096):

| путь        | Q4_0      | Q8_0      |
|-------------|-----------|-----------|
| INT8 AMX    | 145 GOPS  | 190 GOPS  |
| IQK (замена)| ~114 GOPS/поток (~228 на 2) | ~226 |

End-to-end `llama-bench -p 512 -n 128`:

| модель | IQK (AMX off) | AMX on | выигрыш |
|--------|---------------|--------|---------|
| Q4_0   | 103.71 ± 0.11 | 103.36 ± 0.47 t/s | паритет |
| Q8_0   | 102.93 ± 6.37 | 106.84 ± 0.27 t/s | +3.8%, стабильнее |

tg128 не меняется (AMX включается только при M ≥ 8; одиночная генерация
идёт через AVX-512 путь).

## Почему кратного выигрыша нет (и не могло быть)

1. **Умножение не было узким местом.** Пик AMX — тысячи GOPS на ядро, но
   он достижим только при непрерывной подаче данных. Для квантованных
   форматов на каждый блок K=32 нужно: переложить веса в VNNI-порядок
   (~100 машинных операций) и пересчитать int32-тайлы в float со шкалами
   (~450 операций) — против 4 инструкций умножения. Умножитель был занят
   ~12% времени. Мы сравнивали не «умножитель против умножителя», а две
   кухни целиком.
2. **Соперник уже очень хорош.** IQK-путь ik_llama.cpp — вылизанный
   AVX-512 VNNI (~114 GOPS/поток на этих формах), и у него структурный
   козырь: веса преупакованы ОДИН раз при загрузке модели (Q8_2_X4).
   Наш INT8-путь перекладывает B при каждом вызове. AVX-512 VNNI тоже
   делает 32 int8-умножения за инструкцию — «маленькая печь без очередей»
   конкурирует с «большой печью, которую вечно ждёт заготовка».
3. **fp16-шкалы каждые 32 числа — фундаментальное свойство формата**,
   а не дефект реализации: с ними INT8-путь обречён на долю «помощи»
   ~в 10 раз больше доли умножений. Это общий потолок схемы и у
   llama.cpp/amx (у апстрима та же структура).

## Вердикт для Q4_0/Q8_0

- Q4_0: паритет. Держать как opt-in (env), в основной путь не навязывать.
- Q8_0: +3.8% в pp512 — первый честный выигрыш, но скромный.
- Расширять INT8-путь на новые квантования (Q4_1, Q4_K, Q5_K, Q6_K,
  IQ4_XS) **бессмысленно**: у них та же структура «блок + шкала», тот же
  потолок, а IQK их уже покрывает и без потери качества. Копировать
  территорию IQK без структурного преимущества — работа ради работы.

## Где AMX всё-таки может дать кратный выигрыш

> **Замечание о тестовой VM (сентябрь 2026):** сервер amx-zainullin —
> KVM-гость, у которого CPUID рекламирует `amx_bf16`, но инструкция
> `tdpbf16ps` вызывает #UD (проверено изолированным пробником; см.
> quirks.md, раздел 11). BF16-ветка на этой VM неисполнима — кернел
> написан и ждёт bare metal; в коде есть исполнительная проба
> (`ggml_amx_has_bf16`) с автоматическим откатом на INT8, так что на
> подобных машинах ничего не падает.

1. **BF16-путь (реализован, проверяется).** Шкалы вшиваются в веса один
   раз при упаковке, накопление fp32 живёт в тайле весь K — «помощь»
   исчезает, тело цикла становится чистым умножением. Это режим, в котором
   AMX насыщается (так работают oneDNN-бенчмарки с цифрами в разы выше
   наших). Ожидание: >200 GOPS на кернеле. Цена: веса толстеют до 2 байт
   и слегка округляются до bf16 (~0.2% на значение) — нужно подтверждение
   по PPL. Ниша: качество уровня FP16 при скорости AMX; для Q8_0-моделей
   память вырастет вдвое, для FP16 — размер тот же, скорость выше.
2. **Преупаковка весов с кэшем** (как у IQK/апстрима): паковать B один
   раз на тензор и переиспользовать между вызовами. Снимает и VNNI-пак, и
   bf16-пак из устойчивого режима инференса. Наибольшая польза — повторные
   вызовы (декодинг, батчи предзаполнения).
3. **BF16 + преупаковка вместе** — кандидат на «кратный выигрыш»: чистый
   тайл-движок + ноль работы при вызове.

## Итог

AMX на этой машине (2 ядра, Sapphire Rapids, q4/q8-форматы) не даёт
кратного ускорения, потому что кратного ускорения не даёт ни один
движок, стоящий за конвейером распаковки квантованных весов: выигрыш
определяется не скоростью умножителя, а тем, сколько «помощи» требует
формат. INT8-путь остаётся включаемой опцией; расчёт на BF16-ветку
(умножение без «помощи») и преупаковку весов.
